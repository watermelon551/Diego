# Owner Acceptance Note

## What Is Now Ready For Thin Shell Consumption

The following Diego-owned pieces are now ready for later thin shell consumption:
- `generation result` truth
- generic content-object output such as `content_blocks_v1`
- source-conditioned drafting inputs
- sectioned structured draft output
- `draft_package` as the current neutral reading of plan plus draft plus revision handoff
- grounded section/block revision semantics
- explicit lifecycle and failure truth for current Diego-owned draft flows

## What Is Still Provisional

The following remain provisional but valid:
- `/v1/content/runs`
- `/v1/content/runs/prompt`
- source-conditioned long-form drafting as a top-level non-PPT Diego surface
- `draft_package` as a public transport implementation, not as a final unified non-PPT surface
- `structure_expansion` as a provisionally implemented generic primitive, not as a settled public contract
- `item_generation` as a provisionally implemented generic primitive, not as a settled public contract

## What Must Not Be Consumed As Stable Yet

Thin shell consumers should not yet treat the following as fully settled:
- the final long-term transport shape for non-PPT Diego surfaces
- `item_generation` as a stable Diego handoff
- `structure_expansion` as a settled public contract
- `sequence_or_script_plan` as an approved public primitive
- any markdown-first interpretation of Diego truth

## What Pressure Sample Should Be Used Next

The next useful pressure sample should come from a second non-identical artifact-first consumer that still needs:
- structured draft truth
- grounded inputs
- bounded revision

It should not require Diego to own compile/export, grading truth, graph editing, or host workflow semantics.
